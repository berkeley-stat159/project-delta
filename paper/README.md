## Knit Project Final Report

The following commands can be used to generate report:

- `make all`: Create all text files and PDF of the report

- `make clean`: Opt to remove intermediate textfile and PDF


